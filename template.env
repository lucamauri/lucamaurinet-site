# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Future secrets go here (e.g. a contact form API key, analytics token, etc.)
# For now the site needs no secrets, but the file is here for when you do.